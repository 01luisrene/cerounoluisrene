<nav class="pagination" role="navigation">
  {{#if prev}}
    <a class="newer-posts active" href="{{page_url prev}}"><span class="icon-chevron-left"></span></a>
  {{/if}}
  <span class="page-number">Página {{page}} de {{pages}}</span>
  {{#if next}}
    <a class="older-posts active" href="{{page_url next}}"><span class="icon-chevron-right"></span></a>
  {{/if}}
</nav>